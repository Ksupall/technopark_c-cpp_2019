#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define ERR -1
#define MEM_ERR -2

int main () {
	setbuf(stdout, NULL);
	printf("Enter string you want to find in file: ");
	char *buffer[10];

	return 0;
}
